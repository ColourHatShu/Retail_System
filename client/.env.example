# Where the Express API lives. Leave unset in development: Vite proxies /api to localhost:5000.
# For a hosted build, set it to the deployed server, e.g. https://pos-api.example.com/api
# VITE_API_BASE=/api
